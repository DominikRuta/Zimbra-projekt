{% from "macros/fields.tmpl" import render_input_field, render_password_field,
                                    render_submit %}
{% extends "shared/layout.tmpl" %}

{% block title %}Register{% endblock %}

{% block content %}
  <h3>List of all accouts</h3>
 <table >
      <tr><th>Account</th><th></th><th>Action</th></tr>
    {% for n in data %}
            <tr><td style="padding: 10px">{{ n[1] }}</td><td>{{ n[2] }}</td><td><a href="{{url_for('auth.edituserzimbra', id=n[0])}}">Editovat</a></td><td><a href="{{url_for('auth.deleteuserzimbra', id=n[0])}}">Smazat</a></td></tr>
    {% endfor %}
    </table>
{% endblock %}